server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RahulNimbokar101/application.yml/blob/main/application.yml
          clone-on-start: true